export function addNumbers(a: number, b: number): number {
  return a + b;
}

// testing whether global eslint-rules are applied in plain JS-Libs: hover :)
function functionWithoutReturnType() {
  return null;
}
